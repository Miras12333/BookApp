@model BookViewModel
<h1 class="display-6 mt-2 text-danger">Удаление книги!</h1>
<hr />

<form asp-area="Admin"
      asp-controller="Books"
      asp-action="Delete"
      method="post">
    <div class="row">
        <div class="col-9">
            <div class="row">
                <div class="col">
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Название книги:</label>
                        <label>@Model.Name</label>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Количество на складе:</label>
                        <label>@Model.Stock</label>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Цена:</label>
                        <label>@Model.Price</label>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Количество страниц:</label>
                        <label>@Model.PageCount</label>
                    </div>

                </div>
                <div class="col">
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Год издания:</label>
                        <label>@Model.EditionYear</label>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Номер издания:</label>
                        <label>@Model.EditionNumber</label>
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-danger fw-bold">Статус подтверждения:</label>
                        <label>@(Model.IsApproved ? "Подтверждено" : "Не подтверждено")</label>
                    </div>
                </div>
            </div>
            <div class="row mb-3">
                @foreach (var image in Model.Images)
                {
                    <div class="col-2">
                        <img src="~/images/books/@image.Url" class="img-thumbnail" width="100" alt="...">
                    </div>
                }
            </div>
            <button type="submit" class="btn btn-danger">Удалить</button>

        </div>
        <div class="col-3">
            <h5 class="text-danger">Категории</h5>
            <ul>
            @foreach (var category in Model.Categories)
            {
                <li>@category.Name</li>
            }
            </ul>
            <h5 class="text-danger">Авторы</h5>
            <ul>
                @foreach (var author in Model.Authors)
                {
                    <li>@author.Name</li>
                }
            </ul>
        </div>
    </div>

</form>
@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial");
}
